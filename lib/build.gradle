apply plugin: 'com.android.library'
apply plugin: 'it.scoppelletti.spaceship.library'

group 'it.scoppelletti.spaceship'
description 'Spaceship Library'
archivesBaseName = 'spaceship'

android {
    compileSdkVersion 25
    buildToolsVersion "26.0.0"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 25
        versionCode 1
        versionName "1.0.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
        vectorDrawables.useSupportLibrary = true
    }
    buildTypes {
        release {
            minifyEnabled false
            consumerProguardFiles 'proguard-rules.pro'
            debuggable false
        }
        debug {
            minifyEnabled false
            debuggable true
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    dataBinding {
        enabled = true
    }
}

spaceship {
    developer {
        name 'Dario Scoppelletti'
        email 'dario@scoppelletti.it'
        url 'http://www.scoppelletti.it'
    }
    copyright '2013-2017'
    pom {
        url 'http://github.com/dscoppelletti/spaceship'
        licenses {
            license {
                name 'The Apache License, Version 2.0'
                url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
            }
        }
        scm {
            connection 'scm:git:git@github.com:dscoppelletti/spaceship.git'
            developerConnection 'scm:git:git@github.com:dscoppelletti/spaceship.git'
            url 'git@github.com:dscoppelletti/spaceship.git'
        }
    }
}

dependencies {
    compile "com.android.support:appcompat-v7:25.3.1"
    compile "com.android.support:design:${versionMap.android_support}"
    compile "com.android.support:recyclerview-v7:${versionMap.android_support}"
    compile "com.android.support:preference-v7:${versionMap.android_support}"
    compile "com.android.support:preference-v14:${versionMap.android_support}"
    compile "com.android.support:support-annotations:${versionMap.android_support}"
    compile "io.reactivex.rxjava2:rxandroid:${versionMap.rxandroid}"
    compile "io.reactivex.rxjava2:rxjava:${versionMap.rxjava}"
    compile "org.greenrobot:eventbus:${versionMap.eventbus}"
    compile "org.slf4j:slf4j-api:${versionMap.slf4j}"
    provided "org.projectlombok:lombok:${versionMap.lombok}"
    annotationProcessor "org.projectlombok:lombok:${versionMap.lombok}"
}
